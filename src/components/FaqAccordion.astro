---
import FaqItem from "./FaqItem.astro";

interface Item {
	q: string;
	a: string;
}

interface Props {
	items: Item[];
}

const { items } = Astro.props;
---

<div class="faq-accordion">
	{items.map((item) => <FaqItem question={item.q}>{item.a}</FaqItem>)}
</div>
